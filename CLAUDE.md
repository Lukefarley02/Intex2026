# CLAUDE.md — AI Agent Instructions

This file is read automatically by Claude Code and other AI coding agents. It tells the agent how this project works so it can be productive immediately. Other AI tools (Copilot, ChatGPT, Cursor, etc.) should also read this file first.

---

## Project summary

University capstone (INTEX W26) — a full-stack nonprofit case management and donor management web app. Graded across four classes simultaneously: IS 401 (project mgmt), IS 413 (full-stack dev), IS 414 (security), IS 455 (machine learning). **Due Friday April 10, 2026 at 10:00am.**

## Repository layout

```
Website/Intex2026/
├── backend/          .NET 10 Web API (C#)
├── frontend/         React + Vite + TypeScript
├── ARCHITECTURE.md   Project structure, packages, and schema gap table
├── CONTRIBUTING.md   How to add a feature end-to-end
├── API_REFERENCE.md  Existing endpoints and response shapes
├── DATA_DICTIONARY.md  Full schema → model mapping with status
└── Intex2026.sln     Solution file
```

One level up (workspace root):
```
lighthouse_schema.sql       Full SQL Server DDL (17 tables)
lighthouse_csv_v7/          17 CSV seed data files
plan.md                     Single source of truth — all decisions, progress, requirements
```

## Critical rules — read before changing anything

1. **plan.md is the source of truth.** Read it before starting work. Do not contradict decisions logged there.
2. **Do not invent an organization name.** It is TBD. Use a placeholder like "[Org Name]" if needed.
3. **Password policy values — do not suggest defaults.** The class requires exceeding ASP.NET Identity defaults. The team will set specific values. Do not hardcode password rules.
4. **.NET 10 requires Swashbuckle for Swagger.** It is no longer built-in. The NuGet package `Swashbuckle.AspNetCore 7.2.0` is already in the csproj. Do not remove it.
5. **Content-Security-Policy must be an HTTP header, not a meta tag.** Already wired in Program.cs middleware. Graders check browser dev tools.
6. **Sensitive fields are admin-only.** `notes_restricted` (residents) and `medical_notes_restricted` (health records) must never be returned to Staff or Donor roles.
7. **Every delete must have a confirmation dialog.** No silent deletes anywhere.
8. **No secrets in the repo.** Connection strings with real credentials go in appsettings.*.local.json (gitignored) or environment variables. Never commit .env files.
9. **lighthouse_schema.sql is the canonical schema.** When expanding models, match column names, types, and constraints from that file exactly.
10. **Frontend proxy assumes backend on https://localhost:5001.** If the backend port changes, update `frontend/vite.config.ts` proxy target.
11. **Frontend build is `tsc --noEmit && vite build`, NOT `tsc -b`.** `tsc -b` is build mode for composite projects with references; our `tsconfig.json` is flat. Do not change this script back.
12. **Do not run `npm audit fix --force`.** It wants to upgrade vitest 2 → 4 (a two-major-version jump) to silence 5 moderate advisories that all trace back to a single esbuild dev-server flaw (GHSA-67mh-4wv8-2f99). The flaw is dev-only and does not ship to production. Decision is to defer until after submission. See the "Known dependency advisories" note below.

## Documentation maintenance — required after every change

Any AI agent or developer working on this project MUST update the relevant documentation 
files after making any change. Do not consider a task complete until the docs reflect 
the current state of the code.

### When to update each file

**Update `CLAUDE.md`** when:
- A new technology, tool, or package is added to the project
- A new critical rule needs to be enforced
- The current state section changes (something moves from 🔲 to ✅)
- A new role, test account, or auth pattern is added
- The project structure changes significantly
- Any new convention or pattern is established that future agents should follow

**Update `ARCHITECTURE.md`** when:
- A new file or folder is created
- A new controller is added
- A new model is created
- A package is added or removed
- The schema gap table changes (a table moves from Missing to Stub to Complete)

**Update `API_REFERENCE.md`** when:
- A new endpoint is created
- An existing endpoint's request or response shape changes
- Auth requirements change on any endpoint
- An endpoint is deleted or deprecated

**Update `DATA_DICTIONARY.md`** when:
- A model is expanded to include more columns
- A table's status changes (Missing → Stub → Complete)
- A new model is created
- Column mappings change

**Update `CONTRIBUTING.md`** when:
- The process for adding a new feature changes
- New steps are required in the development workflow
- New gotchas or common errors are discovered

### How to update

At the end of every task, before reporting completion:
1. Review which files were created or modified
2. Identify which documentation files are affected using the guide above
3. Update those documentation files to reflect the current state
4. In your completion summary, explicitly list which docs were updated and what changed

### Current state tracking

The "Current state" section in CLAUDE.md uses this format:
- ✅ = fully built and tested
- 🔲 = not yet started or not yet complete

Always update this section to reflect reality. If something is partially done, 
keep it as 🔲 and add a note explaining what's left.

### Never leave docs stale

If you find that the documentation doesn't match the code, fix the documentation 
as part of your current task even if you weren't asked to. Stale documentation 
causes every future agent to make wrong assumptions and waste time.

## Tech stack (locked — do not suggest alternatives)

| Layer | Technology |
|---|---|
| Backend | .NET 10 / C# / EF Core |
| Frontend | React 18 + TypeScript + Vite 6 |
| Routing | react-router-dom v6 |
| Styling | Tailwind CSS 3 + shadcn/ui (Radix primitives) |
| Icons | lucide-react |
| Server state | @tanstack/react-query |
| Forms | react-hook-form + zod |
| Testing (frontend) | Vitest 2 + @testing-library/react + jsdom |
| Database | SQL Server (Azure SQL for production) |
| Auth | ASP.NET Identity + JWT Bearer (fully implemented & audited) |
| Swagger | Swashbuckle.AspNetCore 7.2.0 |
| Deployment | Microsoft Azure |
| ML | Python / Jupyter notebooks (separate ml-pipelines/ folder) |

## Coding conventions

### Backend (C#)
- Namespace: `Intex2026.Api.*` (e.g., `Intex2026.Api.Models`, `Intex2026.Api.Controllers`)
- Models: PascalCase properties, one class per file, file name matches class name, `[Table]` and `[Column]` attributes for explicit SQL mapping
- Table mapping: `modelBuilder.Entity<X>().ToTable("snake_case_table_name")` in AppDbContext (plus `[Column("snake_case")]` on each property)
- Controllers: RESTful, `[ApiController]` attribute, route pattern `api/[controller]`
- All async — use `async Task<ActionResult<T>>` pattern

### Frontend (TypeScript/React)
- Functional components only — no class components
- Hooks for state: `useState`, `useEffect`; server state via `@tanstack/react-query`
- Pages in `src/pages/`, shared components in `src/components/`, shadcn primitives in `src/components/ui/`
- Use the `@/` path alias (configured in `tsconfig.json` and `vite.config.ts`) for all internal imports
- Styling is **Tailwind utility classes only** — do not write custom CSS files. Use the theme tokens from `src/index.css` (e.g. `bg-primary`, `text-secondary`, `gradient-ember`) instead of hardcoded colors so the Ember palette stays consistent
- Use shadcn/ui components from `@/components/ui/*` (Button, Card, Input, Label, Dialog, etc.) instead of building from scratch
- Authenticated pages should be wrapped in `<ProtectedRoute roles={[...]}>` in `App.tsx` and use `<DashboardLayout title="...">` for the sidebar shell
- Public pages should use `<PublicNav />` at the top
- Auth state via `useAuth()` from `@/api/AuthContext`; data fetching via `apiFetch<T>()` from `@/api/client` (auto-attaches the JWT)

## Frontend rewrite history (Apr 7 2026)

The original plain-CSS frontend was wholesale replaced with the **ember-hope-flow** design system to give the app a polished, donor-facing look. The previous `src/` is preserved at `frontend/src_old_backup/` if anything needs to be recovered.

What changed:
- Tailwind + shadcn/ui added; Inter font, Ember palette (warm orange primary, deep teal secondary, gold accent), gradient utilities
- 12 new pages from ember-hope-flow ported into `src/pages/` (Index, Donate, Login, Dashboard, Donors, Safehouses, Residents, Reports, StaffPortal, DonorPortal, Admin, NotFound), plus `Register.tsx` and `Privacy.tsx` rewritten in the new style
- React downgraded 19 → 18 and react-router 7 → 6 to match shadcn/ui ecosystem expectations
- `AuthProvider` moved inside `BrowserRouter` so `Login` can call `useNavigate` on success
- Only `Login.tsx` and `Register.tsx` are wired to the real backend right now. All staff/admin/donor pages still render mock data and need follow-up work to swap mock data for `apiFetch()` calls against the existing controllers.
- `Dashboard.tsx` (admin/staff landing) and `DonorPortal.tsx` were redesigned on Apr 7 2026 to match new reference mockups. Dashboard now leads with four KPI cards (Active Donors, Total Donations YTD, Monthly Donations, Donor Retention), a Social Media Overview card, Recent Activity feed, and three quick-link cards (Manage Donors, Case Management, View Reports). DonorPortal now leads with a full-width ember-gradient hero banner showing donor name, girls helped, and total contributions, followed by three stat cards (Girls Helped, Total Given, Donations Made), a Donation History table, and an Active Campaigns list with progress bars and Contribute buttons. Both pages use only Tailwind utility classes and the existing Ember theme tokens (`bg-primary-light`, `text-secondary`, `gradient-ember`, etc.) — no new CSS files.
- Same day (Apr 7 2026) both pages were wired to the live Azure SQL backend via `@tanstack/react-query` + `apiFetch()`. Dashboard pulls from the new `GET /api/dashboard/stats` endpoint (active donors, YTD/monthly donation sums with YoY and MoM deltas, rolling 12-month donor retention, and the 6 most recent donations joined to supporter names). DonorPortal pulls from the existing `GET /api/donorportal/me`, `/me/impact`, `/me/donations` endpoints plus the new `GET /api/campaigns` endpoint which aggregates distinct `campaign_name` values from the donations table into raised totals (goals are synthesized as ~1.5× raised, rounded to the nearest $5k, since no campaigns table exists in the schema). The only remaining mock content is the Social Media Overview card, because `social_media_posts` is not yet modeled. "Girls Helped" on DonorPortal is derived client-side from `total_donated / 1500` with a floor of `campaigns_supported.length`. New backend files: `backend/Controllers/DashboardController.cs`, `backend/Controllers/CampaignsController.cs`, and the previously empty `backend/Controllers/SupportersController.cs` was filled in with a standard CRUD pattern (Admin/Staff read/write, Admin-only delete).
- Also Apr 7 2026: the remaining mock-data pages were audited and wired to Azure. `Residents.tsx` now fetches `/api/residents` via react-query, mapping arbitrary `case_status` strings into one of four display stages (Intake / Program / Exit prep / Follow-up) through a `deriveStage()` helper, and colour-codes a risk badge from `current_risk_level`. `Safehouses.tsx` now fetches `/api/safehouses` (new controller — LEFT JOINs residents and returns `activeResidents` alongside `capacityGirls`, `storedOccupancy`, and the full city/province/region/country tuple; full CRUD with Admin-only DELETE). `Admin.tsx` now pulls users from a new `/api/adminusers` endpoint (uses `UserManager<IdentityUser>` to enumerate every Identity account with their roles) and derives its three KPIs from live data: donor retention from `/api/dashboard/stats`, safehouse utilization from `sum(activeResidents)/sum(capacityGirls)` across `/api/safehouses`, and program completion from the share of residents whose `caseStatus` contains "closed"/"reintegrated"/"exited"/"follow". `Index.tsx` (public landing) now pulls from two new `[AllowAnonymous]` endpoints on a new `PublicController` — `/api/public/stats` drives the three hero pills (girls supported, safehouse count, rolling-12-month retention rate matching the admin dashboard) and `/api/public/safehouses` drives the safehouse grid (name, city, region, capacity, active count only — no resident-identifying data). The only pages still on mock data are `Reports.tsx` and `StaffPortal.tsx` (no `reports` or `weekly_logs` tables in the schema yet) and the Social Media Overview card on the admin Dashboard (no `social_media_posts` model yet) — everything else is live Azure SQL. New backend files from this pass: `backend/Controllers/SafehousesController.cs`, `backend/Controllers/AdminUsersController.cs`, `backend/Controllers/PublicController.cs`.
- Also Apr 7 2026: the last two IS 413 required pages — **Process Recording** and **Home Visitation & Case Conferences** — were built end-to-end. Two new controllers `ProcessRecordingsController` and `HomeVisitationsController` provide full CRUD over the existing `process_recordings` and `home_visitations` models (Admin/Staff read+write, Admin-only DELETE; the `notes_restricted` column on process recordings is only returned when the caller is in the Admin role). POST handlers generate the next PK server-side because both tables use non-identity INT primary keys in the canonical schema. Two new frontend pages `ProcessRecording.tsx` and `HomeVisitation.tsx` consume these endpoints via react-query, each with a resident filter, a "new record" modal (shadcn `Dialog`), and a chronological card list. `HomeVisitation.tsx` additionally splits entries into an "Upcoming case conferences & visits" section and a past "Visit history" section based on `visitDate`. Both pages are protected by `ProtectedRoute roles={["Admin","Staff"]}` at `/process-recording` and `/home-visitation`, and the sidebar in `DashboardLayout.tsx` now links to them with `NotebookPen` and `MapPin` icons. Also wired: `RoleSeeder.cs` was extended so that on startup the `donor@ember.org` test account is guaranteed to have a matching `MonetaryDonor` row in `supporters` plus four historical donations totalling $2,350 across four campaigns (idempotent — the seeder skips donation insertion if any already exist for that supporter). Test credentials for admin/staff/donor are documented in `README.md`. All passwords satisfy the hardened password policy (length ≥ 12, upper, lower, digit, non-alphanumeric).
- Also Apr 7 2026: **four-tier access control was enforced end-to-end on the backend.** A new `backend/Authorization/UserScope.cs` helper resolves the calling principal into one of `Founder` (Admin with no Region/City — sees everything), `RegionalManager` (Admin with Region only — safehouses/residents/supporters scoped to that region), `LocationManager` (Admin with both Region and City — sees the single safehouse + its residents/visitations/process recordings; supporters scoped by region), `Staff` (Staff role with City — same case-management scoping as LocationManager but **cannot see monetary or in-kind donors at all** and `notesRestricted` is stripped from every resident and process-recording response), and `Donor` (case data hidden entirely; only the donor portal endpoints are usable). The helper exposes `ApplyToSafehouses` / `ApplyToResidents` / `ApplyToSupporters` that wrap an existing `IQueryable<T>` with the right `Where` clause, and `CanAccessSafehouseRow(...)` for single-row authorization checks. Every controller that touches case management or supporters now takes `UserManager<ApplicationUser>` in its constructor and calls `UserScope.FromPrincipalAsync` at the top of each action: `ResidentsController`, `SafehousesController`, `SupportersController`, `ProcessRecordingsController`, `HomeVisitationsController`, `DashboardController`, and `AdminUsersController`. **Founder-only actions:** creating safehouses, deleting any record (resident, safehouse, supporter, process recording, home visitation), and updating any other user's region/city via `PUT /api/adminusers/{id}/scope`. Region/Location managers retain full read+write inside their scope but cannot widen it (e.g. PUT on a safehouse cannot move it to a different city/region). The dashboard returns all-zero monetary KPIs to Staff so the page still renders without leaking giving data, and `Staff` callers never receive `MonetaryDonor` or `InKindDonor` rows from `/api/supporters` even if they request those types in the query string. The `adminScope` field on `GET /api/auth/me` was renamed from `"company"` to `"founder"` to match the new four-tier model. No schema changes were needed — the existing `Region` and `City` columns on `ApplicationUser` already supported this; the previous behaviour just never enforced them on the read paths. While doing this work several controller files were found to be truncated mid-statement on disk (a stale working tree from a prior session); they were restored from `git show HEAD:` before being modified.
- Also Apr 7 2026: **safehouse occupancy is now kept in sync with the residents table automatically.** The `safehouses.current_occupancy` column was drifting from reality because the seed CSVs were authored independently of the residents data — e.g. Lighthouse Safehouse 2 showed `current_occupancy = 8` in the DB but only had 3 residents with `case_status = 'Active'`, so the Safehouses page (which counts Active residents live) rendered "3/10". `case_status` has three values (`Active`, `Closed`, `Transferred`); only `Active` represents a girl currently in the house. Fix: `ResidentsController` now has a private `RecalculateOccupancyAsync(int safehouseId)` helper that sets `current_occupancy` to the live count of Active residents for that safehouse, and it's called after every POST, PUT, and DELETE (the PUT path recomputes both the old and new safehouse when a resident is moved between houses). `Program.cs` also runs a one-time backfill on startup after the role seeder: it groups residents by safehouse, and for every safehouse whose stored `current_occupancy` disagrees with the live Active count, it writes the corrected value and logs the number of rows reconciled. This is wrapped in a try/catch so a DB hiccup can't take down the app. No frontend changes needed — `Safehouses.tsx` still prefers `activeResidents` over `storedOccupancy`, and now both values agree.
- Also Apr 7 2026: the Residents page (`Residents.tsx`) gained three client-side filter dropdowns above the resident card list — **Case status** (Any / Active / Closed / Transferred), **Safehouse** (Any + the unique list of safehouse names present in the current `/api/residents` response, computed via `useMemo`), and **Priority** (Any / low / medium / high / critical, matching the `currentRiskLevel` column and the existing `riskColor` map). All three use the shadcn `<Select>` primitive from `@/components/ui/select`. A `__any__` sentinel is used for the "no filter" option because Radix `Select` throws a dev warning on empty-string item values. The filters compose as AND on top of the existing search box, and a "Clear filters" ghost button appears next to the dropdowns whenever any one of them is narrowed. No backend changes — everything filters in memory against the existing query. Also during this session two frontend files (`Safehouses.tsx` and `Residents.tsx`) were found truncated mid-JSX on disk — the same "stale working tree" issue that hit the backend controllers earlier — and were restored from `git show HEAD:` before being modified. When the Edit tool targets lines that exist in a truncated file it silently succeeds without touching the broken tail, so Vite's parser is the first thing that notices. If you see an `Unexpected token, expected "jsxTagEnd"` error at the last non-blank line of a page file, suspect truncation and `git show HEAD:frontend/src/pages/X.tsx > frontend/src/pages/X.tsx` before editing.
- Also Apr 7 2026: **full Create/Edit/Delete CRUD was added to the three core management pages** — Safehouses, Residents, and Donors — and a reusable confirmation dialog component was introduced to satisfy the IS 414 "every delete must have a confirmation dialog" grading rule. New file `frontend/src/components/ConfirmDialog.tsx` wraps shadcn's `AlertDialog` with a simple `{open, onOpenChange, title, description, confirmLabel, destructive, loading, onConfirm}` prop API; the confirm button uses `e.preventDefault()` so the parent mutation controls when the dialog actually closes (via `onSuccess`), letting the user see the "Working…" loading state. Each of the three pages now has: (1) a shadcn `Dialog`-based create/edit modal with react-hook-form-style local state, (2) Pencil/Trash2 icon buttons inline on each row/card, (3) three react-query `useMutation`s (create / update / delete) that invalidate the right query keys on success, and (4) role-gated buttons using `hasRole()` from `AuthContext`. **Safehouses**: form covers name, safehouseCode, city, province, region, country, status (Active/Paused/Closed), openDate, capacityGirls, capacityStaff, notes; write/delete gated to Admin role; `currentOccupancy` is always posted as 0 because `ResidentsController.RecalculateOccupancyAsync` is the source of truth. **Residents**: the edit form only exposes the 10 most-edited fields (caseControlNo, internalCode, safehouseId dropdown populated from `/api/safehouses`, caseStatus, currentRiskLevel, dateOfBirth, dateOfAdmission, sex, assignedSocialWorker, notesRestricted which only renders for Admin) but the form state is a full `ResidentEntity` with `[key: string]: unknown` index signature so the 30+ categorical fields round-trip untouched on PUT; create/edit gated to Admin+Staff, delete gated to Admin only (matches backend `[Authorize(Roles = "Admin")]` on DELETE); both `["residents"]` and `["safehouses"]` queries are invalidated on success because moving a resident between houses or flipping `caseStatus` shifts occupancy on both sides. To make this round-trip work cleanly, `GET /api/residents/{id}` was expanded to return the full `Resident` entity instead of the previous slim projection — `NotesRestricted` is still stripped for non-Admin callers, and the `Safehouse` navigation property is set to null before serialization so the PUT payload stays flat and EF Core doesn't complain about an attached navigation graph. **Donors**: the supporterType dropdown is locked to `MonetaryDonor` / `InKindDonor` (per Appendix A those are the only two supporter types that qualify as "donors"); create/edit/delete gated to Admin only (Staff is already backend-blocked from those supporter types so the buttons would be useless anyway). Validation is minimal but enforces either a personal name (firstName + lastName) or an organizationName, plus required email, region, and country. During this pass ~9 files were found truncated mid-statement on disk (`Donors.tsx`, `ProcessRecording.tsx`, `HomeVisitation.tsx`, `Admin.tsx`, `App.tsx`, `DashboardLayout.tsx`, `Dashboard.tsx`, `DonorPortal.tsx`, `package.json`) — same stale-working-tree issue as before, all restored from `git show HEAD:` before editing. TypeScript compile (`npx tsc --noEmit`) passes clean.
- Also Apr 7 2026: `Donors.tsx` was wired to the real backend. The page was previously a hardcoded array of exactly 8 mock donors. It now fetches `/api/supporters?types=MonetaryDonor,InKindDonor` — per Appendix A of the case doc, only `MonetaryDonor` and `InKindDonor` qualify as "donors" out of the six `supporter_type` values. `SupportersController.GetSupporters` was extended with an optional comma-separated `types` query filter and a LEFT JOIN on the donations table so every row returns `totalDonated`, `donationCount`, and `lastGiftDate` aggregates. The Donors page shows three filter pills (All / Monetary / In-Kind) with counts, a search box, and an "At-risk" toggle. The risk badge (Active / Watch / At risk / Dormant) is derived client-side from days since `lastGiftDate` (<60 / 60–89 / 90+ / never). The admin dashboard's `Active Donors` KPI was re-tuned to the same `MonetaryDonor + InKindDonor` filter so the two pages agree.
- Apr 8 2026: **The sidebar in `DashboardLayout.tsx` is now role-filtered.** The static `navItems` array was extended with a `roles: Array<"Admin"|"Staff"|"Donor">` tag per item, and the component computes `visibleNavItems` via `useMemo` using `hasRole()` from `AuthContext`. Precedence is Admin → Staff → Donor-only (Admins who also hold the Donor role still see the full admin nav because Admin outranks Donor). Concretely: a Donor-only user sees nothing in the sidebar except **Donor Portal**. A Staff user sees only **Safehouses, Residents, Process Recording, Home Visitation, Staff Portal** — Dashboard, Donors, Reports, ML Insights, and Admin are hidden because Staff is backend-blocked from donor data and those pages are either monetary or admin-only. Admins see every item. This matches the four-tier access-control rules in `UserScope.cs` and prevents confused-deputy clicks where Staff or Donor users would land on a page the backend silently blanks. No routing changes — `ProtectedRoute` still guards each URL, so typing one in directly still 403s. Frontend tsc --noEmit is clean.
- Apr 8 2026: **Self-service public donation flow + US tax-receipt generation.** The previously-mocked Donate page was wired to real backend for the first time, and donors can now generate their own IRS-style written acknowledgment letters from the donor portal. New backend endpoint `POST /api/public/donate` on `PublicController` ([AllowAnonymous]) accepts `{firstName, lastName, email, amount, monthly, isAnonymous, campaignName}` and either reuses an existing supporter by email or creates a new `MonetaryDonor` row (region `Online`, country `United States`, channel `Website`), then inserts a `Donation` row. Both `supporter_id` and `donation_id` are non-identity INT PKs so the handler generates them server-side via `MaxAsync + 1` (same pattern as ProcessRecordings / HomeVisitations). Anonymous donations create a synthetic `anonymous-<guid>@ember.local` supporter with `status = "Anonymous"` so the donations.supporter_id FK stays valid without leaking PII. Non-anonymous donors who later register with the same email see their entire donation history in the donor portal automatically because `DonorPortalController` already matches by email — no linking table needed. The `Donate.tsx` page was rewritten: it now POSTs to `/api/public/donate`, then shows a shadcn `Dialog` listing four benefits of creating an account (track impact, download tax receipts, full donation history, manage recurring gifts). "No thanks" leaves the donation saved anonymously; "Yes" opens a second dialog with an inline password prompt (client-side validation matches the hardened policy: length ≥ 12, upper, lower, digit, symbol), POSTs `/api/auth/register`, then calls `login()` from `AuthContext` and routes to `/my-impact`. A new dismissible first-visit donate banner was added to the top of `Index.tsx` for unauthenticated visitors, remembered in sessionStorage via the key `donate-banner-dismissed`. For the tax-form feature, a new endpoint `GET /api/donorportal/me/tax-receipt?year=YYYY` returns everything needed to render an IRS Publication 1771 written acknowledgment letter — organization block (name, EIN placeholder, address, contact), donor block, tax year, per-gift line items, total, the standard "no goods or services were provided" disclosure, and a `formReference` string noting this acknowledgment is what donors use with **Schedule A (Form 1040)** when itemizing charitable contributions. (There is no numbered IRS form for cash donation acknowledgments; Form 8283 only applies to non-cash gifts over $500, which this flow does not collect. The 501(c)(3) written acknowledgment is what every US nonprofit actually issues.) The endpoint defaults the window to the current year, and the year filter uses explicit `>= yearStart && < yearEnd` bounds rather than `DateTime.Year` to avoid EF Core translation issues on a nullable DATETIME2 column. A new `frontend/src/pages/TaxReceipt.tsx` page (route `/tax-receipt`, protected for Admin/Staff/Donor) fetches this endpoint with a year-picker dropdown and renders a print-friendly letterhead, salutation, contributions table, IRS disclosure, and signature block. "Print / Save as PDF" triggers `window.print()`; browsers natively offer "Save as PDF" from that dialog, so no server-side PDF library was needed. A "Generate tax receipt" button was added to the `DonorPortal.tsx` hero next to total contributions that links to the new page. Placeholder values (`Org name`, `EIN XX-XXXXXXX`, mailing address) are clearly marked in the backend response and will be filled in before go-live once the real org info is available. Frontend `tsc --noEmit` is clean. During this pass ~6 files were again found either truncated mid-JSX or padded with trailing null bytes on disk (`Dashboard.tsx`, `Donors.tsx`, `MLInsights.tsx`, `Residents.tsx`, `Safehouses.tsx`, and `components/SafehouseMap.tsx`) — the same stale-working-tree issue documented earlier; all were restored from `git show HEAD:` before continuing.
- Apr 8 2026: **Admin sidebar was condensed into collapsible categories + Staff got their own dashboard.** (1) `DashboardLayout.tsx` now renders the sidebar as a set of standalone top-level links plus collapsible `NavGroup`s (Case Management, Fundraising, Analytics, System). Each group has an icon header with a rotating chevron and auto-expands the section that owns the current route; other groups remember their last open/closed state. (2) `StaffPortal.tsx` was deleted and the `/staff` route removed from `App.tsx`. A new `StaffDashboard.tsx` page took its place — it renders a profile card (email, role, region, city, and this-month event tallies), a safehouse card (name, city/region/country, status badge, occupancy progress bar, code, staff capacity, open date; the backend already scopes `/api/safehouses` by city so Staff only ever see their assigned safehouse), a merged "upcoming" + "recent history" event feed built from `/api/homevisitations` and `/api/processrecordings`, and three quick-action cards (Residents, Process Recording, Home Visitation). Home visitations with a `visitType` containing "conference" or "review" are classified as **case conferences**; process recordings with a `sessionType` containing "weekly" or "check" are classified as **weekly check-ins**, so the old StaffPortal weekly check-in UX has a home without needing a new table. Events are normalized into a single `StaffEvent` type and sorted by date, then split into upcoming (future, soonest-first) and history (past, newest-first). (3) `App.tsx` gained a tiny `DashboardRouter` component that reads `useAuth().hasRole("Admin")` and renders `<Dashboard />` for Admins or `<StaffDashboard />` for everyone else on `/dashboard`. (4) The `/safehouses` route is now **Admin-only** (`roles={["Admin"]}`), because Staff see their safehouse embedded on the Staff Dashboard instead — `DashboardLayout.tsx` matches by dropping Safehouses from the Staff sidebar filter. (5) The sidebar's `topLinks` now includes Dashboard for both Admin and Staff. Frontend `tsc --noEmit` is clean.
- Apr 8 2026: **ML Insights is now Founder-only on both the nav and the in-page prompt cards.** The backend already distinguished `Founder` (Admin with no Region/City) from `RegionalManager` / `LocationManager` via `UserScope.cs`, and `/api/auth/me` already returned `adminScope: "founder" | "region" | "location" | null`. The frontend now honours that. `AuthContext.tsx` was extended: the `User` type gained `adminScope`, `region`, and `city`; the context exposes a new `isFounder` boolean (true only when `adminScope === "founder"`). Both the mount-time `/api/auth/me` validation path AND the `login()` flow now populate these fields — `login()` issues a second `fetch("/api/auth/me")` after the token is stored so scope info is available immediately. `ProtectedRoute.tsx` gained a `founderOnly` prop that renders an "Access Denied — restricted to top-level administrators" screen when set and the user isn't a Founder. `App.tsx` changed the `/ml-insights` route from `roles={["Admin","Staff"]}` to `roles={["Admin"]} founderOnly`. `DashboardLayout.tsx` added an optional `founderOnly?: boolean` field on the `NavItem` type and tagged the ML Insights sidebar link with it; `visibleTopLinks` and `visibleGroups` now additionally filter out any item whose `founderOnly` is true when the user isn't a Founder. The four pages that had inline ML Insights prompt cards linking into the ML Insights tabs — `Dashboard.tsx` (4-card row: churn / capacity / outcomes / geographic), `Donors.tsx` (Pipeline 01 Churn, 02 Capacity, 06 ROI — previously gated on `canWrite = hasRole("Admin")`), `Residents.tsx` (Pipeline 04 Outcomes), and `Safehouses.tsx` (Pipeline 05 Geographic) — all now wrap those sections in `{isFounder && (...)}`. Regional Managers, Location Managers, Staff, and Donors no longer see ML buttons anywhere in the app, and typing the URL directly lands on the founder-only Access Denied screen. No backend changes were needed. Frontend `tsc --noEmit` is clean.
- Apr 8 2026: **Reports & Analytics was wired end-to-end to real data**, closing the last IS 413 mock-data gap. New `backend/Controllers/ReportsController.cs` exposes a single scope-aware endpoint `GET /api/reports/summary?start=&end=` (default window is the trailing 12 months) that returns everything the IS 413 rubric asks for in one payload: donation trends (monthly time series, by type, by campaign, allocation by safehouse weighted by residents served), resident outcomes (case status, case category, sub-category breakdown by the Philippine social-welfare taxonomy — trafficked, OSAEC, CICL, etc.), safehouse performance comparison (capacity / active / residents served / closed / utilization per safehouse), reintegration success (closed count, successful count, rate, reintegration-type breakdown), and the Annual Accomplishment Report framing (Caring / Healing / Teaching pillars: girls served + counseling sessions + home visits + risk-level improvements + successful reintegrations). Scope handling mirrors `DashboardController` — Founders see everything, Region/Location managers see their slice, and Staff callers get `staffView: true` with empty monetary sections so the page still renders without leaking donation data. The old `Reports.tsx` was entirely replaced — the previous version was a hardcoded `pastReports` array plus a mock "auto-generated summary"; it now fetches the new endpoint via react-query with a live start/end date picker and renders six sections (Annual Accomplishment header, Donation trends with an inline Tailwind bar chart, By type / By campaign / Allocation across safehouses, Safehouse performance table, Resident outcomes, and Reintegration success). Frontend tsc --noEmit is clean. Note the donation-by-safehouse allocation is synthesized: the schema has no first-class donation→safehouse link, so each safehouse's share is weighted by the number of residents it served inside the window.
- Apr 8 2026: **Donation confirmation ding + honest impact metrics.** (1) `Donate.tsx` now plays a cheerful two-note C6→E6 chime via the Web Audio API the instant `/api/public/donate` returns 2xx (no audio asset shipped — it's synthesized with `OscillatorNode` + `GainNode`, wrapped in a try/catch so audio failures never block the thank-you screen). The `AudioContext` is instantiated inside the submit handler so iOS/Safari's user-gesture requirement is satisfied, and the context is closed ~900ms after the chime to avoid leaks. (2) The "girls helped" metric was reworked to stop misleading donors. Previously `ImpactCalculator.GirlsHelped` = `round(donated / costPerGirl)`, which meant a $25/month donor giving for 10 months ($250 total) showed 0 girls helped, while a lump-sum $1,500 donor showed 1 — the math was honest but the framing was confusing. New model: `cost_per_girl` (≈$1,500) represents a *full year* of care, so `monthly_cost_per_girl = cost_per_girl / 12` (≈$125/month). New method `ImpactCalculator.MonthsOfCare(donated, costPerGirl)` returns `donated / monthly_cost` rounded to 1 decimal — a linear metric that always rewards every dollar. `GirlsHelped` was redefined as `floor(months_of_care / 12)` with a floor of 1 for any gift of $25+, so it now means "full girl-years of care funded" — a recurring donor supporting the same girl for ten months shows 10 months of care + 0 (or 1, with the floor) girl-years funded, not 10 separate girls. Both fields plus `monthly_cost_per_girl` are returned from `/api/donorportal/me/impact` and `/api/public/stats`. (3) `DonorPortal.tsx` now shows four stat cards (Care Provided as primary, Girl-Years Funded as secondary, Total Given, Donations Made) and the hero banner reads "Your giving has funded X months/years of shelter, meals, counseling, and schooling — the equivalent of fully supporting N girls for a full year." `formatMonths(m)` helper renders as "0.2 mo", "5 mo", or "1.2 yrs" depending on size. (4) `Index.tsx` landing page hero pill was changed from the derived `girlsHelped` to the actual `girlsSupported` count (raw count of residents ever served in the DB) and relabeled "girls sheltered" — an honest, non-derived number that matches what a visitor would see walking through the safehouses. (5) `Donate.tsx` impact preview was also rewritten: monthly gifts now show the annualized months/years of care ($25/mo × 12 = 2.4 months of care for a girl-year), and one-time gifts still show days using the new `$1500/365` daily rate instead of the stale `0.83` magic number. No schema changes. Frontend `tsc --noEmit` is clean. During this pass `ImpactCalculator.cs`, `DonorPortalController.cs`, `PublicController.cs`, `Donate.tsx`, `DonorPortal.tsx`, `Index.tsx`, and `tsconfig.json` were again found truncated on disk — same stale-working-tree issue documented repeatedly above; all were restored from `git show HEAD:` before editing.
- Apr 8 2026: **In-person donation logging flow + temporary-password donor onboarding.** `MonetaryDonor` and `InKindDonor` supporters are both now loggable from the admin Donors page and the Staff Dashboard via a new `POST /api/donations` endpoint + a shared `LogDonationDialog` wizard. New backend controller `backend/Controllers/DonationsController.cs` (Admin+Staff) takes either an existing `supporterId` or an inline `newSupporter` payload plus the gift details, validates donation type (`Monetary` / `InKind`, with amount required for monetary and estimatedValue required for in-kind), region-scopes everything through `UserScope` (Staff/Regional/Location cannot move a supporter outside their own region), and generates non-identity PKs via MaxAsync+1 for both `supporters` and `donations`. This is the **only** path by which Staff can create `MonetaryDonor`/`InKindDonor` rows — `SupportersController`'s direct POST still blocks them from those types. For in-kind gifts the item description is stashed in `donations.notes` because no `in_kind_donation_items` table exists yet. On the frontend `Donors.tsx` renders a narrow Staff-only early return (a single "Log a donation" hero button + a muted `Lock` note explaining the donor roster is admin-only) while Admins still see the full donor grid with both "Log donation" (outline) and "Add donor" (hero) buttons in the header. `StaffDashboard.tsx` adds a 4th quick-action tile that opens the same dialog. The dialog (`frontend/src/components/LogDonationDialog.tsx`) is a 5-step wizard — type → match/search existing donors → create new donor → gift details → success — built on shadcn `Dialog` with react-query invalidation of `["supporters","dashboard","donorportal"]` on save. **Tax receipt response was split** — `GET /api/donorportal/me/tax-receipt?year=YYYY` now returns separate `cashDonations` + `nonCashDonations` arrays with their own totals, plus `form8283Required` (true when any single in-kind gift exceeds $500) and a dedicated `nonCashDisclosure` string. `TaxReceipt.tsx` renders two distinct tables (Cash Contributions with a totals row; Non-Cash with a "Donor estimate*" column whose asterisk explains the charity does not certify fair-market value), an amber Form 8283 notice box when any non-cash gift is >$500, and splits the IRS disclosure into cash and non-cash paragraphs. `DonorPortal.tsx` donation history now shows a gold "In-Kind" badge + the item description (from `notes`) + an "est. value" caption for in-kind entries. **Temporary-password onboarding:** when the staff log-donation flow creates a *new* supporter AND staff supplied a valid email, the backend also provisions a matching ASP.NET Identity account via `UserManager<ApplicationUser>`, assigns it the `Donor` role, and marks it with a new `MustChangePassword` flag (new column on `AspNetUsers` via migration `20260408120000_AddMustChangePassword` — auto-applied on startup by the existing pending-migrations loop in `Program.cs`). `DonationsController.GenerateTempPassword()` uses `RandomNumberGenerator.GetInt32` to produce a 14-character seed that satisfies the hardened policy (4 upper + 4 lower + 4 digit + 2 symbol, ambiguous glyphs 0/O/1/l/I excluded, Fisher-Yates shuffled). The generated password is returned **once** in the `/api/donations` response alongside `donorAccountEmail`, and the dialog transitions to a dedicated success step that displays it in a `<code>` block with a copy-to-clipboard button and a warning that the value is not recoverable. On the donor side, `AuthResponse` / `GET /api/auth/me` now include `mustChangePassword`; `AuthContext` exposes it as `mustChangePassword` + a `clearMustChangePassword()` helper + a `setToken()` helper; `ProtectedRoute` hard-redirects any signed-in user with the flag to `/account` regardless of which URL they typed; `AccountSettings.tsx` shows an amber `ShieldAlert` banner when the flag is set and, on successful password change, swaps in the fresh JWT returned by `POST /api/auth/change-password`, clears the flag, and routes the donor to their portal. `AuthController.ChangePassword` was upgraded to always clear `MustChangePassword` on success, rotate the security stamp (invalidating any *other* active JWTs for that account), and return a fresh `AuthResponse` so the caller isn't bounced to the login screen mid-session. The password hint text in AccountSettings was also tightened from 12 to 14 characters to match the policy actually enforced in `Program.cs`. Frontend `tsc --noEmit` is clean.

- **In-app donor messaging system (Apr 9 2026)**: Admins can now send templated in-app messages to donors directly from the Donors page. The previously non-functional `Send` icon button on each donor row now opens a `SendMessageDialog` with two template options — **Thank You** (auto-fills donor name, last gift date, and total giving into a gratitude letter) and **Donation Appeal** (encourages another contribution with the correct $25/month anchor rate). Templates are fully editable before sending. A **bulk send** button ("Message all" / "Message N") appears in the top bar next to "Log donation" and "Add donor" — it messages every donor currently visible in the filtered list, so admins can filter by type or at-risk status and then blast the filtered set. On the backend, a new `DonorMessage` model (table `donor_messages`) stores each message with `supporter_id`, `template_type`, `subject`, `body`, `is_read`, `created_at`, and `read_at`. The table is created on app startup via raw SQL `IF NOT EXISTS` in `Program.cs` (no formal EF Core migration required). `DonorMessagesController` (Admin-only) exposes `POST /api/donor-messages` (single) and `POST /api/donor-messages/bulk`. `DonorPortalController` gained three new endpoints: `GET /api/donorportal/me/messages` (newest first), `PUT /api/donorportal/me/messages/{id}/read`, and `PUT /api/donorportal/me/messages/read-all`. On the donor side, `DonorPortal.tsx` shows a **collapsible Messages strip** above the Donation History / Active Campaigns grid. The strip has a single-line header with an unread count badge and a chevron toggle; clicking it expands a height-capped (max 192px) scrollable message list. Clicking a message opens its full body below the list. The bottom grid stays a clean two-column layout. Message timestamps are parsed with a `Z` suffix appended when missing (backend returns `DateTime.UtcNow` without trailing `Z`, which JavaScript would otherwise interpret as local time — this was causing a ~300-minute offset for US Central time zones). The `donor_messages` table uses non-identity INT PKs generated via `MaxAsync + 1`, matching the pattern used by donations and process recordings. The old single "At-risk" toggle button on the Donors page was replaced with four risk-filter pills — **All**, **Active**, **Watch**, **At risk** — so admins can segment donors by engagement level before sending bulk messages. New files: `backend/Models/DonorMessage.cs`, `backend/Controllers/DonorMessagesController.cs`, `frontend/src/components/SendMessageDialog.tsx`. Modified files: `AppDbContext.cs` (registered `DonorMessages` DbSet + table mapping), `DonorPortalController.cs` (+3 endpoints), `Donors.tsx` (wired Send button + bulk send + risk filter pills + dialog instances), `DonorPortal.tsx` (+messages query, mark-read mutations, collapsible inbox strip), `Program.cs` (+donor_messages table ensure on startup). Frontend `tsc --noEmit` is clean.

- **Case Conferences & Intervention Plans (Apr 9 2026)**: Staff and admins can now plan and track case conferences and intervention plans directly from a new dedicated page. New `InterventionPlan` model (table `intervention_plans`) stores conference metadata with columns: `plan_id` (non-identity PK via MaxAsync+1), `resident_id` (FK), `plan_category`, `plan_description`, `services_provided`, `target_value` (decimal), `target_date`, `status`, `case_conference_date`, `created_at`, `updated_at`, and `created_by_user_id` (added via startup SQL `IF NOT EXISTS`). The backend `InterventionPlansController.cs` (Admin+Staff) exposes full CRUD under scope, with per-author ownership for Staff edit/delete — admins can modify any plan in scope, but staff can only edit/delete the records they personally created. `GET /api/interventionplans?residentId=N` filters by optional resident and orders by conference date descending. `Program.cs` auto-applies the `created_by_user_id` column on startup. The frontend `CaseConferences.tsx` page (`/case-conferences`, protected for Admin+Staff) groups plans by `case_conference_date` into chronological conference blocks, filters by category/status/resident using shadcn `Select` dropdowns, renders summary stat cards (total plans, by status, by category), expands each conference to show its resident + plan cards, and opens a detail sheet or edit dialog on click. Full CRUD is wired: a create button opens a `Dialog` with react-hook-form covering resident, date, category, description, services, target value/date, status; Edit/Delete icon buttons on each plan row trigger the same dialog or a `ConfirmDialog` per IS 414 requirements. `DashboardLayout.tsx` sidebar gained a **Case Conferences** link under the Case Management group with a Gavel icon, visible for Admin+Staff only. `AppDbContext.cs` registered `InterventionPlan` with `decimal(10,2)` for target_value. New files: `backend/Models/InterventionPlan.cs`, `backend/Controllers/InterventionPlansController.cs`, `frontend/src/pages/CaseConferences.tsx`. Modified files: `AppDbContext.cs`, `Program.cs`, `App.tsx` (added /case-conferences route), `DashboardLayout.tsx` (added sidebar link). Frontend `tsc --noEmit` is clean.

---

## Frontend test infrastructure (Apr 7 2026)

Vitest is wired up in `frontend/vite.config.ts` using `jsdom` as the test environment, with `src/test/setup.ts` loaded before each test file (the setup imports `@testing-library/jest-dom` so matchers like `.toBeInTheDocument()` are available globally). `globals: true` is enabled so tests can use `describe`/`it`/`expect` without importing them, though explicit imports also work. The `types` array in `tsconfig.json` lists `vitest/globals` and `@testing-library/jest-dom` so TypeScript picks up the right ambient types.

Run tests with `npm test` (watch mode) or `npm run test:run` (single pass). Only a placeholder `src/test/example.test.ts` exists right now — real component tests still need to be written.

---

## Known dependency advisories (deferred)

`npm audit` reports 5 moderate-severity advisories, all stemming from a single root cause: **GHSA-67mh-4wv8-2f99** in `esbuild <=0.24.2`. The other 4 entries (`vite`, `vite-node`, `@vitest/mocker`, `vitest`) are transitive parents of the same vulnerable `esbuild`.

**Scope:** The vulnerability affects esbuild's development server only. It allows a malicious webpage visited in the same browser as a running Vite dev server to read responses from that server. It does not affect production builds, which are static assets served from Azure.

**Impact on this project:** Development-only; no production exposure. The dev server binds to localhost and is only running during active development on the team's machines.

---

## Latest frontend updates (Apr 8 2026 - continued)

- **Donation flow consolidated (Apr 8 2026)**: Registration page (`Register.tsx`) was removed entirely — the new account creation pathway is **exclusively through the donation flow**. When a donor completes their donation, they're prompted to create an account with an inline password dialog (no separate registration page). `Login.tsx` updated to direct new users to `/donate` instead of `/register`. Password requirement hardened to **14 characters minimum** (was 12, now consistent across donate flow, login hints, and register page password hints in Donate password prompt). `frontend/src/api/client.ts` fixed: API client now correctly uses `/api` proxy in development (routes to `https://localhost:5001`) and production Azure URL in non-dev builds.

- **UI/UX improvements (Apr 8 2026)**: `Donate.tsx` header replaced `<PublicNav />` with a dedicated `<DonateHeader />` component (Ember logo only, no navigation links) for a clean, focused donation experience. All progress bars across the app (Safehouses occupancy, Admin KPIs, Donor campaign progress, Staff safehouse occupancy) changed from **orange-filled / teal-empty to green-filled / gray-empty** for better visual clarity of progress. Updated `src/components/ui/progress.tsx`: background changed to `bg-muted` (light gray), indicator changed to `bg-success` (green).

- **Visibility-triggered animations (Apr 8 2026)**: Animated counters on the homepage (`Index.tsx`) now use `IntersectionObserver` to delay animation until the component becomes visible on screen — statistics no longer count up on page load if the user hasn't scrolled to them. `AnimatedCounter.tsx` wrapped with intersection observer (threshold 0.1); `StatPill.tsx` similarly wrapped so the `animate-count-up` class only applies after visibility. This improves perceived performance and creates a more engaging reveal effect as users scroll.

- **Donor Portal viewport-lock + safehouse-designation polish (Apr 8 2026)**: `DashboardLayout.tsx` gained an optional `fitViewport?: boolean` prop that, when true, switches the main content area from the default `p-6` flow layout to `p-4 h-[calc(100vh-3.5rem)] overflow-hidden`, plus `min-w-0 overflow-x-hidden` on the wrapping main column so no page-level horizontal scrollbar can leak in. `DonorPortal.tsx` ("My Impact") was restructured to use the new prop and now fits the entire experience in one viewport: a flex-column root (`flex flex-col h-full gap-3`) holds a compact hero banner (inlined Total Contributions + Donate/Tax-receipt buttons), a 3-column stat-card row (Care Provided / Girl-Years Funded / Donations Made — Total Given dropped because it's already in the hero), and a single-row inline safehouse strip that replaces the previous full card (`MapPin` + dropdown + Go button), all marked `flex-shrink-0`. The bottom `lg:grid-cols-2` grid wraps Donation History and Active Campaigns in `flex-1 min-h-0` so they split the remaining vertical space; each card is `flex flex-col min-h-0` with a `flex-shrink-0` header and a `flex-1 min-h-0 overflow-y-auto` content area so long histories and long campaign lists scroll internally instead of pushing the page. The donation-history column now shows the full list instead of just the first 5, and campaign cards shrank to `p-3` / `text-sm` with a `line-clamp-2` description to fit more without scroll. `Donate.tsx` "Donate to a safehouse" picker was simultaneously swapped from the previous grid of big button tiles to a shadcn `Select` dropdown that mirrors the one on the Donor Portal verbatim — same "Name — City, Region" option format, same placeholder states for loading/empty. The `Select` imports were added to `Donate.tsx` and the old tile grid (with its per-button `Check` selection affordance) was removed; the underlying `selectedSafehouse` state and `?location=` pre-seeding still work unchanged so the backend `POST /api/public/donate` payload is identical. Finally, the donation history table now labels safehouse-designated gifts correctly: `PublicController` writes `donations.notes` as `"Online donation via /donate · Designated for: <safehouse>"` and always sets `CampaignName` to the literal string `"General Fund"` when no campaign was chosen (it's never null). `DonorPortal.tsx` gained an `extractDesignatedSafehouse()` helper whose regex (`/Designated for:\s*([^·|;]+?)\s*$/i`) finds the marker *anywhere* in the notes (the first try used `^Designated for:` which never matched because the backend prefixes the marker with the base note + middle-dot separator), and the rendering now treats any case-insensitive `"General Fund"` campaign name as the "no specific campaign" sentinel. Result: a safehouse-designated gift with no campaign shows `"<Safehouse Name> General Fund"`, a safehouse-designated gift with a real campaign shows `"<Safehouse Name> — <Campaign Name>"`, and everything else renders as before. Frontend `tsc --noEmit` is clean.

- **Full CRUD on Process Recording + Home Visitation (Apr 8 2026)**: Both pages previously only supported Create + Read. They now have Edit and Delete wired end-to-end against the existing `PUT /api/processrecordings/{id}` + `DELETE /api/processrecordings/{id}` (and the matching home-visitations endpoints). Backend already exposed these — PUT is Admin+Staff, DELETE is Admin-only (Founder-only per `UserScope.IsFounder` check inside the delete handler) — so no backend changes were needed. Frontend changes: `ProcessRecording.tsx` and `HomeVisitation.tsx` each gained a `FormState` interface with an ID field (0 when creating), a `toFormState(row)` helper, a `saveMut` mutation that POSTs when `recordingId`/`visitationId === 0` and PUTs otherwise, and a `deleteMut` mutation. `apiFetch<void>` already handles 204 responses correctly. The dialog title/footer button label swap between "New / Save Recording" and "Edit / Save Changes" based on `editingId`. Each row in the list renders `Pencil` and (Admin-only) `Trash2` icon buttons in the card header; edit loads the row into the form and reopens the dialog, delete opens the shared `ConfirmDialog` component (wrapping shadcn `AlertDialog` per the IS 414 "every delete must have a confirmation dialog" rule). ProcessRecording also exposes an explicit **Session Date** field in the form (previously the POST silently stamped `Date.now()`); HomeVisitation already had a visit-date field. The dialog's `onOpenChange` resets `editingId`/`form` on close to avoid leaking state between create/edit sessions. During this pass several files (`PublicNav.tsx`, `AccountSettings.tsx`, `DonorPortal.tsx`, `Index.tsx`, `Residents.tsx`) were again found truncated mid-JSX on disk — same stale-working-tree issue documented repeatedly above — and were restored from `git show HEAD:` before re-running tsc. Frontend `tsc --noEmit` is clean.
- **Admin Dashboard viewport-lock + cleanup (Apr 8 2026)**: `Dashboard.tsx` (admin/staff landing) received the same treatment as the Staff Dashboard — it now uses `DashboardLayout`'s `fitViewport` prop with a flex-column root (`flex flex-col h-full gap-3`). The KPI metric row and the Founder-only ML Insights row are `flex-shrink-0` and use tightened `p-3` cards (was `p-5`), `text-2xl` values (was `text-3xl`), `w-7 h-7` icon wells (was `w-9 h-9`), and `gap-3` (was `gap-4`). The Social Media Overview + Recent Activity grid is `flex-1 min-h-0` with each card set to `flex flex-col min-h-0`, a `flex-shrink-0` header, and a `flex-1 min-h-0 overflow-y-auto` content area so the long platform breakdown and activity feed scroll internally instead of pushing the page. The **three bottom quick-link cards (Manage Donors, Case Management, View Reports) were removed** because every one of them duplicates a sidebar link and adds a full row of vertical space for no new information. `ArrowRight`, `Heart`, `FileText`, and the `quickLinks` array were deleted as dead code. No data/logic changes — same queries, same KPIs, same ML tiles, same activity feed. Frontend `tsc --noEmit` is clean.
- **Staff Dashboard viewport-lock + cleanup (Apr 8 2026)**: `StaffDashboard.tsx` was condensed to fit the viewport without a vertical scrollbar. It now uses `DashboardLayout`'s `fitViewport` prop with a flex-column root (`flex flex-col h-full gap-3`); the greeting row, profile card, and safehouse card are `flex-shrink-0` and the events grid is `flex-1 min-h-0` with each card's content area set to `flex-1 min-h-0 overflow-y-auto` so long upcoming/history lists scroll internally. Spacing, icon sizes, text sizes, and card padding were all tightened (e.g. event rows went from `p-3` + `w-9 h-9` icon wells to `p-2` + `w-7 h-7`). The **four quick-action tiles at the bottom were removed** (My residents, Process recording, Home visitation, Log donation) because three of them duplicate sidebar links and the fourth — Log donation, Staff's only path into the donation flow — was relocated to a primary `Log donation` button in the dashboard greeting row so Staff can still reach it from one click. A **`View residents` button** was added to the "My safehouse" card header (routes to `/residents`, which is already backend-scoped to the Staff user's city so they only see their own safehouse's residents). `ArrowRight` and `AlertTriangle` imports + the footer weekly-check-in nudge were removed as dead code. Frontend `tsc --noEmit` is clean.
- **Home page hero rework + sticky header fix + animated percentages + dark-mode contrast (Apr 8 2026)**: Several small but visible polish passes on `Index.tsx` and `PublicNav.tsx`. (1) **Sticky header fix**: the first-visit donate banner lived in document flow *above* a `sticky top-0 z-50` `<PublicNav>`, so when a user scrolled back up after scrolling down, the banner would re-emerge at the top but the still-stuck nav would overlap it — the nav appeared "out of place" on the return journey. Fix: both the banner and `<PublicNav>` are now wrapped in a single `sticky top-0 z-50` container inside `Index.tsx` so they travel together as one unit; PublicNav's own inner sticky becomes a no-op inside the stuck parent. (2) **Hero wordmark**: the hero headline was restructured. A large serif "Ember Foundation" wordmark now sits above the headline (`text-5xl md:text-6xl lg:text-7xl font-serif font-extrabold`). "Ember" is rendered in the darker end of the gradient-ember palette (`text-[hsl(11_63%_38%)]`) to match the "Support a girl's journey" button directly beneath it, and "Foundation" is rendered `text-white dark:text-black` per the user's explicit request (yes, black-on-dark-hero in dark mode — acknowledged as a legibility tradeoff but honored literally). The previous "Ember Foundation empowers NGOs…" paragraph under the headline was removed. The headline itself was shrunk from `text-4xl md:text-5xl lg:text-6xl` to `text-2xl md:text-3xl lg:text-4xl` so it reads as a subtitle under the wordmark, and was broken into three lines via explicit `<br />` tags: "Every girl deserves" / "a safe place" (in `text-primary`) / "to heal & grow". (3) **PublicNav trim**: the top-bar brand label was shortened from "Ember Foundation" to just "Ember" (the Flame icon stays) since the home page hero now carries the full wordmark. PublicNav is used on every public page so this affects Login, Privacy, etc. as well. (4) **Animated percentage counters**: the three percentage stats in the "How we care" section (progress rate, positive-end rate, favorable-visit rate) were previously rendered as static `${Math.round(care.progressRate * 100)}%` strings while the integer stats next to them counted up from 0 via `<AnimatedCounter>`. The `AnimatedCounter` component already exposed a `suffix` prop, so the three percentages now use `<AnimatedCounter value={care ? Math.round(care.progressRate * 100) : undefined} suffix="%" />` and animate up to their target when scrolled into view, matching the integer stats. (5) **Dark-mode contrast fix on the "How we care" section**: the section had a hardcoded inline `style={{ backgroundColor: 'hsl(11 60% 88%)' }}` (light peach) that didn't switch in dark mode, so dark-mode `text-foreground` (near-white) became white-on-light-peach and was unreadable. Replaced with `bg-[hsl(11_60%_88%)] dark:bg-[hsl(11_40%_14%)]` so the section background morphs to a deep ember tone in dark mode while the inner card `bg-card` + `text-foreground` values continue to adapt naturally. Frontend `tsc --noEmit` is clean. During this pass `Index.tsx` and `FlipCard.tsx` were again found padded with trailing null bytes on disk (same stale-working-tree issue documented repeatedly above) and were cleaned in place with a `d.replace(b'\x00', b'').rstrip()` pass before re-running tsc.
- **Account Settings + full dark mode (Apr 8 2026)**: The sidebar footer in `DashboardLayout.tsx` no longer has a "Sign out" button — it's been replaced with an **Account Settings** link (`/account`) that routes to a new self-service page. Sign out still lives in the top header bar so users can one-click exit from anywhere. The new `frontend/src/pages/AccountSettings.tsx` is authenticated for every role (Admin, Staff, Donor) and offers four sections: (1) an **Appearance** card with three tile buttons — Light, Dark, Same as system — plus a compact `<Select>` fallback for narrow viewports, (2) a **Change email** form that posts to the new `POST /api/auth/change-email` endpoint, confirms the current password server-side, and signs the user out after success so `AuthContext` reloads against the new identity, (3) a **Change password** form that posts to `POST /api/auth/change-password` with client-side enforcement of the hardened policy (length ≥ 12, upper, lower, digit, symbol — Identity re-validates on the server), and (4) a **Delete account** card gated behind a `ConfirmDialog` that requires the current password and hits `DELETE /api/auth/account`; on success the user is logged out and routed to `/`. Error messages from Identity (e.g. "Current password is incorrect.", password-policy failures) are surfaced inline via a small `extractErrorMessage` helper because `apiFetch` normally swallows them. **Dark mode is fully implemented.** A new `frontend/src/api/ThemeContext.tsx` provider persists the preference to `localStorage` under the key `ember-theme`, watches `matchMedia("(prefers-color-scheme: dark)")` live so "system" mode re-evaluates when the OS toggles, and toggles the `dark` class on `<html>` (plus `colorScheme` so native controls/scrollbars follow the palette). `ThemeProvider` is wired at the top of `App.tsx` inside `QueryClientProvider`. The `.dark` palette was added to `src/index.css` next to `:root` and overrides every CSS variable the app uses (`--background`, `--foreground`, `--card`, `--popover`, `--primary`/`--primary-light`, `--secondary`, `--muted`, `--accent`, `--gold`, `--destructive`, `--border`, `--input`, `--ring`, all `--sidebar-*`, `--warning`, `--success`). Values were tuned to preserve the Ember brand identity on dark backgrounds (warmer/brighter primary, dimmed teal sidebar, deeper base) while meeting WCAG AA. The `.gradient-warm` utility was rewritten from hardcoded light-mode HSL values to `hsl(var(--primary-light))` / `hsl(var(--background))` so it morphs with the theme instead of looking out-of-place in dark mode. Tailwind already had `darkMode: ["class"]` configured so no config changes were needed — every `bg-*`, `text-*`, `border-*` utility across the app that uses a theme token now automatically recolors. `ConfirmDialog.tsx` was extended to accept an optional `children` slot so the delete-account dialog can render the password confirmation input inside the dialog body. New backend endpoints in `AuthController.cs`: `POST /api/auth/change-email` (re-issues a JWT after `SetEmailAsync` + `SetUserNameAsync`, checks current password, rejects emails already in use), `POST /api/auth/change-password` (thin wrapper over `UserManager.ChangePasswordAsync`), `DELETE /api/auth/account` (re-verifies password then `UserManager.DeleteAsync`; donation rows are intentionally left intact so historical giving and tax records stay valid). Frontend `tsc --noEmit` is clean. During this pass several files were again found truncated on disk (`AuthController.cs`, `index.css`, `client.ts`, `App.tsx`, `PublicNav.tsx`, `StatPill.tsx`, `ui/progress.tsx`, `Donate.tsx`, `Index.tsx`, `Login.tsx`) — same stale-working-tree issue documented earlier; all restored from `git show HEAD:` before editing.
- **Per-author ownership on Process Recording + Home Visitation (Apr 8 2026)**: Staff can now update AND delete the records they personally created — but only those. Previously PUT was Admin+Staff (any staff in scope could edit any row) and DELETE was Founder-only. New migration `20260408180000_AddCreatedByUserId` adds a `created_by_user_id NVARCHAR(450) NULL` column to both `process_recordings` and `home_visitations`, wrapped in `IF NOT EXISTS` so it is safe against an Azure SQL database that already has the column, and auto-applied on startup by the existing pending-migrations loop in `Program.cs`. The `ProcessRecording` and `HomeVisitation` models gained a matching `CreatedByUserId` property and `AppDbContextModelSnapshot.cs` was updated in place so the snapshot stays in sync. `ProcessRecordingsController` and `HomeVisitationsController` now: (1) stamp `CreatedByUserId = _users.GetUserId(User)` on POST, overwriting whatever the client sent so the owner id cannot be spoofed; (2) return `createdByUserId` plus a server-computed `canModify` flag on every GET list/detail row — admins always get `canModify = true`, staff get it only when the row owner matches their user id; (3) on PUT, reject with 403 when `!scope.IsAdmin && existing.CreatedByUserId != callerId` and always preserve the original owner regardless of what the client dto said; (4) on DELETE, the `[Authorize(Roles = "Admin"]` attribute + the `scope.IsFounder` guard were removed — the delete handler now allows admins (any tier) in scope plus staff who own the row, while still requiring scope access to the parent resident via `CanAccessResidentAsync`. Legacy rows with a NULL owner (pre-migration seed data) are effectively admin-only, which is the safe default. Frontend: `ProcessRecording.tsx` and `HomeVisitation.tsx` both have `createdByUserId` + `canModify` added to their row interfaces; the `isAdmin = hasRole("Admin")` derivations were removed (replaced with a no-op `useAuth()` call so the import still works for future use); Edit/Delete icon buttons are now gated on the per-row `canModify` flag instead of the caller role. `HomeVisitation.tsx`s `VisitCard` child gained an `onEditable?: boolean` prop (default true) so the parent can hide Edit as well as Delete when the caller is not the row author, and the whole action-button cluster is wrapped in `{(onEditable || onDelete) && (...)}` so rows with neither button no longer render an empty flex container. No schema data loss — column is nullable, existing rows are unaffected. Frontend `tsc --noEmit` is clean.
